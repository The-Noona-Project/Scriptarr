# Vault

Vault is Scriptarr's shared auth and state broker over MySQL.

It now stores the Raven VPN settings, Raven metadata provider configuration, Oracle provider state, and Oracle secrets
used by the rest of the stack.
